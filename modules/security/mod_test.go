package security

import "testing"

func TestHeaders(t *testing.T) {
	// Check for "Auth Header" field in custom headers section
	// then verify if true or not.
}

func TestRateLimite(t *testing.T) {
	// Stress test on how the server can handle requests without fail.
}
